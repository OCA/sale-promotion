Go to the Promotional Program form view, and configure the date field to use
when checking a program's validity for a given order.

Possible options are:

* Order date (default, core behaviour)
* Commitment date
